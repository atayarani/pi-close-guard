{
  "name": "pi-close-guard",
  "version": "0.1.0",
  "description": "Confirm before /clear or /new discards a non-empty pi conversation. Configurable prompt.",
  "keywords": [
    "pi-package",
    "pi",
    "extension"
  ],
  "license": "MIT",
  "author": "Ali Tayarani",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atayarani/pi-close-guard.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "format": "prettier --write \"extensions/**/*.ts\" \"test/**/*.ts\"",
    "format:check": "prettier --check \"extensions/**/*.ts\" \"test/**/*.ts\"",
    "check": "npm run typecheck && npm run format:check && npm test"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.3",
    "@types/node": "^26.1.0",
    "prettier": "^3.9.4",
    "tsx": "^4.22.5",
    "typescript": "^6.0.3"
  }
}
